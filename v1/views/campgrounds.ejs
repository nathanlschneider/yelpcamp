<h1>This is the camgrounds page!</h1>

<% campgrounds.forEach(function(campground){ %>
    <div>
        <h4><%= campground.name %></h4>
        <img src="<%= campground.image %>" width = "600px" height = "300px">
    </div>

<% }); %>